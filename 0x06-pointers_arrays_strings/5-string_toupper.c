#include "main.h"

/**
 * string_toupper - A function that converts a string to all uppercase
 * @str - The string to be converted
 * Return: Always the converted string
 */

char *string_toupper(char *str)
{
	return (toupper(str));
}
